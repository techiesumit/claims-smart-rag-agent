# Web/API framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# HTTP client
httpx>=0.27.0

# Data validation
pydantic>=2.7.0

# RAG / LLM tooling (optional but likely for this project)
langchain>=0.2.0
langchain-community>=0.2.0

# Vector DB clients (optional; pick what you use)
pinecone-client>=5.0.0
weaviate-client>=4.6.0

# Database
# (sqlite3 is stdlib; no extra driver needed)

# CLI / UX niceties (optional if you later want pretty CLI)
rich>=13.7.0
typer>=0.12.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

python-multipart>=0.0.11
jinja2